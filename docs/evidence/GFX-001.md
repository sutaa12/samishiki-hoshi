# GFX-001 renderer spike evidence

> **Current verdict: remediation candidate ready for independent re-review.** The first audit at branch head `804a8cb` found S0=0, S1=2, S2=3, S3=1 and correctly kept R2-G1 open. Candidate `f81f383` addresses each finding below; the gate remains `in progress` until a fresh independent review accepts these exact bindings.

## Bound identity

- Remediation implementation commit: `f81f38397c41430ff94e948dde629801cb6eac83`
- Complete source archive SHA-256: `dde8698b09cd5e3883ab8241bc1f94ff73a11ae76eb57aefe6329ddff4e4c6ce`
- Built `dist/**` digest: `8a5d3e22a8beaad9a1508c657d0898fc5efea97a264e64bd5dc18e72a5545cab` over 63 sorted per-file SHA-256 records
- Playwright JSON SHA-256: `a8023056f801e86e90fed0dfcaa46f13718f333416936c30aeca905087711edb`
- Branch/worktree: `graphics-photoreal-megademo` / `/Users/snari/Documents/GitProject/LonelyStar-graphics-photoreal-megademo`
- Frozen release line: `main` at `6c010ee228ace28e655ceeba764513806505b03e`
- `src/game/model.ts` SHA-256: `9aa6b27123431ccde694f0ecd31e03c66093c4aa04575e05399f82fcb75001b2`
- `src/game/simulation.ts` SHA-256: `5aafeb15658888315532fb22176e4da1aec14487dd690866f5456c51ba841fd7`

Reproduction:

- Source: `git archive --format=tar --output=/tmp/lonelystar-f81f383-source.tar f81f383` then `shasum -a 256 /tmp/lonelystar-f81f383-source.tar`.
- Build: checkout `f81f383`, run `rtk npm run build`, then run `rtk node scripts/hash-tree.mjs dist` from the evidence commit that introduced the script.

## Acceptance exercised on 2026-08-18 JST

| Check | Command | Result |
|---|---|---|
| Type safety | `rtk npm run typecheck` | pass |
| Lint | `rtk npm run lint` | pass |
| Production build | `rtk npm run build` | pass; `/gfx-spike` emitted |
| Deterministic and telemetry unit regression | `rtk npm run test -- --run` | 4 files, 18/18 tests pass |
| Backend/lifecycle E2E | `GFX_SPIKE_PORT=3101 rtk npm run test:e2e -- tests/e2e/gfx-spike.spec.ts --project=desktop-chromium` | 5/5 pass; 0 skipped, 0 unexpected, 0 flaky |

The suite requires all of the following:

- `three/webgpu` `WebGPURenderer` initializes asynchronously; observed NodeMaterial inventory is non-zero and every spike material has assigned nodes;
- `renderer.compileAsync(scene, camera)` completed before the recorded first-render start;
- `forceWebGL: true` reports the actual WebGL2 backend and renders non-zero draws, triangles, programs, geometries, attributes, and tracked resource bytes;
- a dedicated Chromium launched with experimental WebGPU flags reports the actual WebGPU backend; fallback is rejected in that lab test;
- browser WebGPU API exposure, a separate `requestAdapter()` probe, and the renderer's actual backend are reported as distinct facts;
- an error emitted after initial telemetry is preserved as a structured event and reaches the live subscriber/UI;
- actual `dispose()` removes the resize listener and event bridge, stops the animation loop, clears runtime subscribers, reduces every renderer resource counter and scene-object count to zero, remains stopped after 200 ms plus a resize event, and is idempotent on a second call;
- an explicit `GFX_SPIKE_PORT` forbids existing-server reuse, while the legacy default-port test behavior remains unchanged.

## First-audit remediation mapping

| Finding | Remediation in `f81f383` |
|---|---|
| S1 disposal/leak stop condition unproved | Localhost-only lifecycle control invokes `dispose()` twice; E2E asserts stopped callbacks/listeners/subscribers and zero post-dispose counters/objects. |
| S1 late errors not observable | Structured event store republishes renderer/device events; focused unit and E2E cover late delivery and field preservation. |
| S2 unsafe WebGPU proof over-claimed | Telemetry and page label the route `experimental-webgpu-lab-only`; the test claims actual lab WebGPU only, not default/native/High/hardware support. |
| S2 API exposure contradicted adapter reality | `webgpuApiExposed`, `webgpuAdapterProbeAvailable`, navigator-probe identity, and actual backend authority are separated. |
| S2 stale server and incomplete binding | Explicit test ports disable reuse; complete source archive, built artifact, and Playwright report have SHA-256 bindings above. |
| S3 TSL/compile fields self-attested | Runtime scene traversal inventories NodeMaterials and assigned nodes; ordered timestamps prove precompile resolution precedes first render. |

## Observations and limits

- This host's experimental Chromium lab path observed SwiftShader WebGPU, and a separate local probe observed Apple `metal-3`. The automated assertion is actual WebGPU backend only; neither observation is a supported-default, High-tier, reference-device, or performance result.
- Single-run initialization, compile, and first-render durations are diagnostics only. They are not substituted for the Notion-defined P50/P95/P99 frame, main-thread, or GPU gates.
- GFX-001 proves one runtime can dispose cleanly and idempotently. It does **not** claim the R2-G6 ten-restart monotonic-growth gate.
- The v1 `ShaderMaterial` visual implementation is incompatible with `WebGPURenderer`. The spike imports no v1 world or visual asset and uses observed TSL NodeMaterials only.
- Reference-hardware performance remains `HARDWARE_PENDING`; `main`, the saved Sites version, and production deployment remain frozen.
