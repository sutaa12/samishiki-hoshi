# GFX-001 renderer spike evidence

## Bound identity

- Implementation commit: `8ead1545b6235eaaba5cf05e0319bea858bcefb1`
- Branch/worktree: `graphics-photoreal-megademo` / `/Users/snari/Documents/GitProject/LonelyStar-graphics-photoreal-megademo`
- Frozen release line: `main` at `6c010ee228ace28e655ceeba764513806505b03e`
- Playwright JSON SHA-256: `777a3d9b261efd9c722ad43b1707a7f2d4e011db136a0458e5bc105d3f884c84`
- `src/game/model.ts` SHA-256: `9aa6b27123431ccde694f0ecd31e03c66093c4aa04575e05399f82fcb75001b2`
- `src/game/simulation.ts` SHA-256: `5aafeb15658888315532fb22176e4da1aec14487dd690866f5456c51ba841fd7`

## Acceptance exercised on 2026-08-18 JST

| Check | Command | Result |
|---|---|---|
| Type safety | `rtk npm run typecheck` | pass |
| Lint | `rtk npm run lint` | pass |
| Production build | `rtk npm run build` | pass; `/gfx-spike` emitted |
| Deterministic unit regression | `rtk npm run test -- --run` | 3 files, 16/16 tests pass |
| Backend E2E | `GFX_SPIKE_PORT=3101 rtk npm run test:e2e -- tests/e2e/gfx-spike.spec.ts --project=desktop-chromium` | 3/3 pass; 0 unexpected, 0 flaky |

The backend suite requires all of the following:

- `three/webgpu` `WebGPURenderer` initializes asynchronously and compiles a TSL-only scene before first render;
- `forceWebGL: true` reports the actual WebGL2 backend and renders non-zero draws, triangles, programs, geometries, attributes, and tracked resource bytes;
- a dedicated Chromium launched with `--enable-unsafe-webgpu --ignore-gpu-blocklist` reports the actual WebGPU backend; fallback is not accepted in this test;
- the preferred path reports either actual WebGPU or an explicit WebGL2 fallback rather than silently relabeling the backend;
- runtime console and page error collection remains empty;
- `GFX_SPIKE_PORT` accepts only decimal ports from 1024 through 65535, allowing the isolated worktree to avoid the preserved server on port 3000.

## Observations and limits

- Reproducible automated WebGPU evidence used Chromium's SwiftShader adapter. A separate local observation reached Apple `metal-3`; neither observation is a reference-device performance result.
- Single-run initialization, compile, and first-render durations are diagnostics only. They are not substituted for the Notion-defined P50/P95/P99 frame, main-thread, or GPU gates.
- The spike records renderer resource counts but does not claim the R2-G6 ten-restart leak gate; lifecycle/restart sampling remains pending in the shared foundation.
- The v1 `ShaderMaterial` visual implementation is not compatible with `WebGPURenderer`. The spike imports no v1 world or visual asset and uses TSL NodeMaterials only.
- Reference-hardware performance remains `HARDWARE_PENDING`; `main`, the saved Sites version, and production deployment remain frozen.
