# QX-R3-002 rail-flight state and encounter evidence

> **Current verdict: review ready.** The implementation and automated criteria
> pass. Independent review and every downstream/external release gate remain
> pending.

## Bound identity

- Branch: `codex/qx-r3-002-rail-flight-state`
- Base: `5b0650e1d9cdec2eef28291d6b830903e19b7751`
- Implementation: `4658c1871b4df823aa29886cd3828a2bb2b17307`
- Tree: `8275b51fe13324956dcb359be32f8ac16f3033a4`
- Complete 355-file manifest SHA-256:
  `c4b00dc1d9fabad34d20e122c7d655557e9f5d7518f275be1f0fd5e132cc7dc4`
- Preserved 128-entry build SHA-256:
  `6f2f75273d2dea8eaf61620280933e4b9d4e1adf40d1253588248a2b7daa0b27`
- Machine receipt: `.quality-gates/receipts/qx-r3-002-review-ready.json`

## Implemented gameplay boundary

- `RailFlightState` extends the renderer-free Journey state with integer-mm
  distance, forward speed, corridor offset, score, Life Chain, Flow Purity,
  mistakes, assist state, cooldown/slowdown, active encounter, one-shot result
  IDs, and an immutable GameplayEvent ledger.
- Story time still owns the exact 180-second six-phase journey. Gate, obstacle,
  and node results use rail distance plus the two corridor axes.
- Gates resolve once as pass or miss. Obstacles resolve once as hit, near miss,
  or silent clear using full 3D endpoint separation. A hit slows forward motion
  for 650ms and then recovers.
- Fractional millimetres carry across simulation slices, so canonical integer
  `distanceMm` does not change when one second is partitioned into 60 steps.
- A pulse starts a 700ms cooldown. Only a Life Node within its 3D good radius
  creates a TwinkleSeed; empty space and cooldown attempts create no Seed.
- Three consecutive misses raise assist and widen only the next Gate by 50%.
  There is no game-over path.
- Normal play no longer synthesizes an Answer at 168.5s. A valid node Pulse in
  ANSWER may set `answerAt`; opt-in auto-give waits for a nearby Life Node.
- The authored story clock now uses the same integer-ms boundary interpretation
  as the v2 WorldPlan, closing the 3-second long-run renderer mismatch found by
  the Graybox recorder.

The temporary default encounter list exists only for R3 graybox validation.
QX-R3-004 must replace it with deterministic WorldPlan-generated encounters;
this task does not claim final level composition or visuals.

## Automated results on 2026-08-22 JST

| Check | Result |
| --- | --- |
| `rtk npm run verify` | pass: typecheck, lint, 39 files / 728 tests, production build |
| Rail browser | 4/4 pass: no-input, empty/node/cooldown Pulse, no auto Answer, node-aware auto-give |
| Production v2 browser | 4/4 pass; actual WebGL2 and host-Metal WebGPU hash `2662d902` |
| Journey regression | 8 pass / 6 intentional project skips / 0 fail |
| Graybox recorder | 1/1 pass; 34.44s, 1280×720 VP8 WebM, 1,068,070 bytes |
| Unit scenarios | endpoint-3D Gate/Obstacle, partition-safe distance, node-only Seed, 3-miss assist, deterministic hash |

The Graybox file is `.quality-gates/qx-r3-002-graybox-30s.webm`, SHA-256
`22f00e92f01c54240c5bc3ee431e3cfa60bc12a31380811167a2655d3b48391f`.
Its 20-second checkpoint is
`.quality-gates/screenshots/qx-r3-002-graybox-20s.png`.

## Limits and stop line

- Independent C3 review rejected the prior source for two S1 and two S2
  findings. All four are preserved in
  `.quality-gates/reviews/qx-r3-002-c3-reject.json` and remediated in
  `4658c18`; fresh acceptance has not run. QX-R3-003 remains stopped until a
  new reviewer reports no open S0/S1/S2.
- The video uses localhost QA speed 10 because software rendering plus video
  capture reduces RAF frequency. Video wall duration is 34.44 seconds; the
  simulation and production paths are unchanged outside localhost QA.
- The current Foundation view is still an architecture/logic graybox, not final
  visual acceptance. Human play and reference hardware remain pending.
- `npm audit` reports 2 low and 15 high inherited dependency findings, 0
  critical. No automatic fix or dependency rewrite was applied.
- Main and public Sites v2 remain unchanged. This source is not deployed.
