# GFX-003 deterministic world-plan evidence

> **Current verdict: artifact review pending.** Implementation
> `c0245667b00e80fe68a511eb0fc206e14f5ead9a` passed three independent source
> reviews with S0=0, S1=0, and S2=0. Promotion remains blocked until the
> preserved evidence bundle is independently reproduced and accepted.

## Bound identity

- Implementation commit: `c0245667b00e80fe68a511eb0fc206e14f5ead9a`
- Implementation tree: `ecb7523dc0cf29fb1ec0c7c96bdb1a56d5c74bb0`
- Complete source archive SHA-256: `c9dee7cea59cc0d5ec7e8cf9ea8b57c3aed9f83ff27135b274ee4b672e1a47f4`
- Complete source archive size: 30,453,760 bytes; 147 entries; no symlinks
- Preserved exact build archive: `.quality-gates/gfx003-c024566-dist.tar`
- Build archive SHA-256: `667625e74215aa7622c8ed897bcfbc99de54bfaa36764227496640f55754c055`
- Build archive size: 4,356,096 bytes
- Archived `dist/**` content digest: `d87b0d8d1e7bf8cbbea1372b4c421effd60642df7b72cad900fcc4468851f8c9` over 70 sorted per-file SHA-256 records
- Archived vinext build ID: `c5ed53b8-41a5-43ea-a2d8-e567d42ebab5`
- Focused report: `.quality-gates/gfx003-c024566-vitest-report.json`
- Focused report SHA-256: `89ac39c0ddc5e395bab2a83cee3a58cb80e753acfb306a1876f52b09e9d34972`
- `package-lock.json` SHA-256: `6711ba750c0c2e1a3a50d20464a24e2cdd4bd1e7236516f9d69a85f0ff3391d8`

The exact build archive was extracted into an empty temporary directory and
reproduced the same 70-file content digest and BUILD_ID with no symlinks.
Vinext rebuilds are not claimed to be byte-reproducible; the reviewed output is
preserved instead.

## Canonical world-plan identity

- Seed: `20260818`
- Generator version: `world-plan-v1`
- Canonical digest: `world-plan-v1:75d93cbb8e0580cd`
- Canonical SHA-256: `4f91cb8393ed54478aa8388600f0329155e0d114fc93b5835305ee9ed7d1642c`
- Canonical byte length: 48,271
- Frozen story-score digest: `story-score-v1:28a70afd6684e90a`

## Automated acceptance exercised on 2026-08-18 JST

| Check | Command | Result |
| --- | --- | --- |
| Complete verification | `rtk npm run verify` | pass: typecheck, lint, 15 files / 350 tests, production build |
| Focused world verification | explicit `tests/world-v2/**` Vitest run with JSON reporter | 6/6 files and 80/80 tests pass; no pending or todo tests |
| Seed properties | `tests/world-v2/world-plan-properties.test.ts` | 1,000 seeds valid |
| Production dependency audit | `rtk npm audit --omit=dev --json` | 0 vulnerabilities |
| Frozen architecture | `tests/world-v2/architecture.test.ts` | frozen game/GFX inputs unchanged; forbidden imports rejected |

## Contract proven

- One canonical chunk exists for every `S01` through `S24`; the 24 contiguous
  nodes cover all six phases and exactly 180 seconds.
- All twelve registered generator systems use named ASCII seed streams derived
  only from the uint32 world seed, system, shot chunk, generator version, and
  optional system-owned substream. Caller context and schedule inputs are
  descriptor-snapshotted once and bounded.
- Generation and chunk schedules do not affect canonical bytes. Quality,
  backend, renderer, DOM, browser, and Three.js state are excluded from the
  world plan and digest.
- Renderer-neutral terrain, water, flora, ecology, atmosphere, and space
  descriptors are present for every chunk. A root library contains exactly the
  seven semantic material families required by the revised renderer contract.
- The canonical score, S08/S14 authored branches, safe corridor, required
  source-to-waterfall-to-outlet hydrology, S20 incomplete peripheral arcs, and
  S21 complete three-shell ship are strictly validated.
- Canonical encoding and validation fail closed with explicit depth, node,
  width, byte, and schedule bounds; hostile caller accessors and Proxies cannot
  induce unbounded semantic rereads.
- Twinkle projection consumes a descriptor-owned snapshot without mutating or
  reordering the frozen ledger. It preserves duplicates, normalizes only
  positional signed zero in the owned semantic copy, and resolves legitimate
  millisecond-rounded phase-boundary pulses without accepting unrelated phase
  mismatches.

## Independent source review

The fixed seventeen-file world-v2 source/test set was reviewed independently by
three agents. All final reviews reported ACCEPT with S0=0, S1=0, and S2=0 and
confirmed exact SHA-256 stability. Fresh attacks covered seed/context ownership,
order independence, work bounds, hostile inputs, story mutations, hydrology,
corridors, descriptors, material families, S20/S21, Twinkle signatures,
signed-zero handling, and all shot/phase boundaries.

## Independent artifact review

Pending. The immutable inputs and review procedure are stored in
`.quality-gates/gfx003-c024566-artifact.json` and
`.quality-gates/gfx003-review-brief.md`. No acceptance receipt exists yet.

## Limits and remaining gates

- This accepts no chunk streaming, worker lifecycle, upload budget, renderer
  material/HDR, temporal pipeline, performance, visual-fidelity, or release
  claim.
- GFX-004 chunk/worker ownership, GFX-005 material/HDR pipeline, GFX-006
  telemetry/parity/restart/performance, and all three Hero Slices remain
  pending.
- Reference-hardware performance remains `HARDWARE_PENDING`.
- Main integration and Sites deployment remain frozen until all three Hero
  Slices pass and the human owner chooses `Merge`, `Partial Merge`, or `Reject`.
