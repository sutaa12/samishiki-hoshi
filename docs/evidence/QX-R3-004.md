# QX-R3-004 WorldPlan encounter evidence

> **Current verdict: REVIEW READY M4.** Automated task criteria pass. Fresh
> independent artifact-only review is required before QX-R3-004 acceptance.

## Bound identity

- Branch: `codex/qx-r3-004-worldplan-encounters`
- Accepted base record: `e657f97b7bb4d36752c375013437ba43c0ca3edf`
- Implementation: `8e7e327e3f473eaf2a2fc114247098c84b21c018`
- Tree: `bd187ae3e336180b643387cbfa34208cb74e234c`
- Complete 390-file manifest SHA-256:
  `81b4734afaeda4bd6b353588ea99e42a2ef606f8a2e914c25a697b4d9f3daeb2`
- Preserved 128-entry build SHA-256:
  `ad22e7fc1ee5d4c938ea64446e6d8040fc75a6bc2204f3a33de93034afbbd93c`
- Machine receipt: `.quality-gates/receipts/qx-r3-004-review-ready.json`

## Implemented encounter boundary

- WorldPlan owns 18 distance-sorted encounter Descriptors and the simulation
  derives its production encounters from the run seed's plan.
- Before 36 seconds the plan contains exactly five Gates, three Obstacles, and
  three Life Nodes. One Tutorial Gate and the first Life Node have complete
  seed-invariant Descriptors, including fixed 3D Flow anchors.
- Every later encounter center and radius comes from the named
  `encounters/placement` stream for its story chunk.
- Each Descriptor pairs collision/perfect radius with visible-ring/near/good
  radius. Rail gameplay and High/Fallback debug markers project that same pair.
- Each Obstacle records an in-bounds safe-route offset. Existing S08 and S14
  branches remain the only authored branches and merge into the story route.

## Automated results on 2026-08-22 JST

| Check | Result |
| --- | --- |
| `rtk npm run verify` | pass: typecheck, lint, 41 files / 743 tests, production build |
| 1,000 canonical seeds | pass: 1,000 checked / 0 invalid |
| First 36 seconds | pass: Gate 5 / Obstacle 3 / Life Node 3 |
| Fixed tutorial equality | pass across seed 0 and 4294967295 |
| High/Fallback descriptor parity | pass |
| Focused production + rail browser | 8 pass / 0 skip / 0 fail |
| Complete manifest | 390/390 tracked files, canonical manifest bound |
| Build preservation | 8,148,992-byte tar, 128 entries, AppleDouble 0 |

Seed `20260818` is pinned to `world-plan-v1:c7ed4025456222e2`.
Its canonical bytes are 53,536 bytes with SHA-256
`d12f0d3ba4072d483cb8bf05a18eeac25e640bc78abc3f0d5dcd41c97c3eb441`.
The quality-independent replay for seed 778 is pinned to `2855bcf2`; the
production 48-second checkpoint is pinned to `fce10451`.

## Browser and laboratory limits

- A locally served exact production build at
  `http://localhost:43160/?seed=20260818` was started and pulsed in the in-app
  browser. It reported the exact plan digest and `node-perfect`.
- The complete 72-case diagnostic initially ran 29 pass, 35 intentional
  project skips, and 8 failures. Four were stale expected values now re-bound.
  The remaining class is the previously disclosed local Graphics laboratory
  budget: three compile warmup samples exceeded 50ms with zero compile
  failures, and one LIFE sample recorded 605 draw calls against the provisional
  600 connection ceiling.
- After rebinding, focused Foundation + Production + Rail ran 9 pass and one
  reproduced atomic compile timing failure. The separately preserved
  Production + Rail report is fully green at 8/8.
- These local measurements do not satisfy the reference-device performance
  gate. `HARDWARE_PENDING` remains unchanged.
- `npm audit` reports 2 low and 15 high inherited dependency findings, 0
  critical. No automatic fix or dependency rewrite was applied.

## Rollback and external gates

Any open S0/S1/S2 finding, invalid seed, blocked route, Gate/Node overlap,
seed-dependent tutorial Descriptor, quality-dependent gameplay Descriptor,
mismatched radius projection, failed focused browser gate, or non-canonical
digest rolls this branch back to accepted base record `e657f97` and blocks
QX-R3-005.

Main and public Sites are unchanged. Human play, rights, reference hardware,
public deployment of this source, and contest submission remain separate and
pending.
