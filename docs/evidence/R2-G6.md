# R2-G6 parity, restart, and release-boundary evidence

> **Current verdict: candidate frozen; independent review pending.** The exact
> implementation `cbbd4e5e0824846c1172c60fa24476ae973de8ca` passes the local
> parity, 1,000-seed, three-Hero, restart, browser, build, and runtime-license
> gates below. This evidence does not accept R2-G6, visual quality, reference
> hardware, public rights, Main integration, Sites deployment, or submission.

## Bound identity

- Implementation commit: `cbbd4e5e0824846c1172c60fa24476ae973de8ca`
- Implementation tree: `85f624d0b0543da91674cf69c60b2ecc3fe0a934`
- Parent Hero C evidence commit: `20dfa3cb082a40b53a947ef72a7ecd81f049faf8`
- Ordered six-file implementation manifest SHA-256:
  `03b100dde94ebdfbe13a79c008638306259c62f5cc245a802b50902e1f36e7e8`
- Complete source archive SHA-256:
  `1e75703d0233ae8982adf609219c73cb42b46e8e35ce70483d38b17f7251985b`
- Complete source archive: 89,845,760 bytes; 274 entries; zero symlinks
- Preserved exact build: `.quality-gates/r2-g6-cbbd4e5-dist.tar`
- Build archive SHA-256:
  `08def5b7e01ad50a1431cead2767003d044c12638b65778d49eccc1d132c9726`
- Build archive: 5,224,448 bytes; 100 regular files; zero symlinks
- Sorted build content-record digest:
  `c65dee41fdf5f41d369c448fed7d253dba78cb677a33ed8748fab4266aedaf53`
- Vinext BUILD_ID: `ed723687-abcb-4f10-95db-d83872b9b3b2`
- Focused Vitest JSON: `.quality-gates/r2-g6-cbbd4e5-vitest.json`, SHA-256
  `85002173e82b22b2445ffcf551fdb5661d2f38fbe2ad6cd3ba2944f91193a1fa`
- Combined Playwright JSON: `.quality-gates/r2-g6-cbbd4e5-playwright.json`,
  SHA-256
  `d914f4c115a758c728f8c3e806fa726ed6e7c55a1f9ca54cfd331914fb26fe7e`
- Production audit JSON: `.quality-gates/r2-g6-cbbd4e5-npm-audit.json`,
  SHA-256
  `41e0957f81e8db3f722663db2e5f7c20bacfd3ceb9caba5820b350f3b86c18bd`
- `package-lock.json` SHA-256:
  `6711ba750c0c2e1a3a50d20464a24e2cdd4bd1e7236516f9d69a85f0ff3391d8`

The archived build was extracted into an empty directory and reproduced the
same 100-file content-record digest, BUILD_ID, runtime notice hash, and
zero-symlink inventory. Vinext rebuilds are not claimed to be byte-reproducible;
the exact tested output is preserved.

## Automated evidence exercised on 2026-08-20 JST

| Check | Result |
| --- | --- |
| Complete verification | `rtk npm run verify`: typecheck, global lint, 36/36 files and 617/617 tests, production build pass |
| Focused R2-G6 oracle | 8 requested files and 54/54 assertions pass; failed, pending, and todo are zero |
| Combined real browser | Foundation plus Hero A/B/C, forced-WebGL2 plus host-Metal WebGPU: 8/8 passed, retry 0, top-level/result errors 0 |
| Production dependency audit | `rtk npm audit --omit=dev --json`: 0 vulnerabilities |
| Browser attachments | four bound telemetry/lifecycle JSON records and twenty embedded 1920×1080 PNGs |

## Determinism and three-Hero isolation

- The frozen journey remains exactly 180 seconds. Seed `20260818` retains the
  canonical plan digest `world-plan-v1:75d93cbb8e0580cd` and the renderer reads
  detached copies rather than mutating the Twinkle ledger.
- The focused parity fixture proves the same gameplay/ledger/world-plan/chunk
  payloads across requested quality and the GFX-006 WebGPU/WebGL2 backend
  matrix. Quality changes never enter the simulation hash.
- The GFX-003 property oracle independently validates 1,000 world seeds and
  rejects timeline, branch, corridor, hydrology, alien-reveal, canonical-number,
  descriptor, and structural-budget mutations.
- One integration test runs Hero A at 12 seconds, Hero B at 75 seconds, and
  Hero C at 176 seconds sequentially through one `Scene`. Each feature survives
  High → Balanced → Low → High without changing its frozen story or ledger
  input. Concurrent disposal is identity-stable, returns geometry/material/
  texture ownership to zero, restores the original background and fog by
  identity, and restores the original scene-child count before the next Hero.

## Restart, browser, and performance evidence

- The forced-WebGL2 live-runtime restart plateau contains ten cycles. Programs
  stay exactly 22, geometries 3, GPU owners 4, logical owners 4, and pool slots
  4 on every cycle. Forced-GC heap use ranges from 18,472,424 to 19,373,388
  bytes, a 900,964-byte spread; this is a browser-lab regression result, not a
  reference-device memory guarantee.
- Ten complete construction/render/disposal generations each end with GPU
  owners, logical owners, pool slots, and retained backend failure references
  at zero; telemetry is disposed and records no runtime spike over 50 ms.
- The steady forced-WebGL2 window has 131 samples, main-thread-work P95
  0.80 ms, and no runtime spike over 50 ms. Its RAF interval P95 is 48.20 ms
  under SwiftShader, so it is not a frame-rate acceptance result.
- The host-Metal WebGPU window has 127 samples, RAF interval P95 9.30 ms,
  main-thread-work P95 0.70 ms, and no runtime spike over 50 ms. GPU timestamp
  support is absent, and this host is not the designated reference machine.
- Hero A/B/C each pass their forced-WebGL2 and host-Metal WebGPU marker routes
  in the same serial run with post-ready program growth, runtime compile events,
  post-initialize Hero allocation, stale chunk activation, and terminal resource
  ownership held at zero by their bound tests.

## Runtime license and provenance boundary

- The complete non-dev `node_modules` runtime closure is React `19.2.6`, React
  DOM `19.2.6`, Scheduler `0.27.0`, and Three.js `0.185.1`; every package
  declares MIT in the lockfile.
- `public/THIRD_PARTY_NOTICES.txt` carries the relevant MIT notice and exact
  versions. The production build contains the byte-identical file at
  `dist/client/THIRD_PARTY_NOTICES.txt`, SHA-256
  `db77168a3a9106f127514a3eff4c59c99872885a85b8baea3ca2c0eaa14fb8ac`.
- `DEMO_SOURCE_PROVENANCE.csv` binds Hero A/B/C to exact implementation commits.
  The Hero code is repository-authored procedural source. Production modules do
  not import the Notion reference-board paths, stock images, models, textures,
  demo shaders, remote fonts, recordings, or a runtime AI service.
- These automated checks establish an engineering inventory only. Final
  public-use and contest-rights attestation remains `HUMAN_PENDING`.

## Interpretation and remaining gates

This run confirms deterministic/backend parity, bounded canonical generation,
three-Hero lifecycle isolation, ten-restart resource plateaus, terminal cleanup,
the compiled Worker, runtime notices, and live route health on the tested host.
It does not independently establish Cinematic Procedural Realism, reference
hardware performance, legal acceptance, or human play acceptance.

The frozen GFX-004/GFX-005, GFX-006, and Hero A/B/C candidates retain their own
`review_pending` receipts. R2-G6 therefore also remains `review_pending` until
a frozen full-scope source/artifact review and blind visual review report no
blocking S0/S1/S2. The release branch remains isolated. Main integration, Sites
save/deployment, and contest submission remain frozen or pending until the
Human Acceptance Owner explicitly chooses `Merge`, `Partial Merge`, or `Reject`.
