# Hero Slice C frozen review brief

Review the exact implementation commit
`69fe2e47c5269ccc407c95a6c6491fbbff1ceb8c` and the evidence commit that adds
this brief. Stop on the first reproducible S0, S1, or S2. Do not edit, rebuild
over the preserved artifacts, or treat browser-lab, AI visual review, or human
gates as interchangeable.

## Reproduce the bindings

1. Verify the eight implementation file hashes and ordered manifest in
   `.quality-gates/hero-c-69fe2e4-artifact.json`.
2. Reproduce `git archive` for the implementation commit: SHA-256
   `bad0fdf9399afdb474e7dd9f81f47cf7c2a0d549b29f77fecbdb31cd7c04fdc4`,
   75,540,480 bytes, 264 entries, zero symlinks, embedded commit `69fe2e4`.
3. Extract `.quality-gates/hero-c-69fe2e4-dist.tar` into an empty directory and
   verify archive SHA-256
   `9eddb1b93ad5998730a981bcdf05e90d1a54df816ec216d390e8ecab32567379`,
   99 files, zero symlinks, content-record digest
   `9f8393d53669553bf5c48e0c00a1742b0eed5c654cb0be1b3cf7ba8e9e6b4a0c`,
   BUILD_ID `1a90dd05-1a36-4560-be80-0340c9d303dd`, and compiled-JS-only
   Worker outputs.
4. Verify the focused Vitest JSON is 2 files / 10 assertions with no failed,
   pending, or todo tests.
5. Verify the Playwright JSON is 2/2 passed, retry 0, error fields 0, and has
   nine embedded 1920×1080 PNGs whose hashes match the artifact manifest.

## Functional attack surfaces

- Exact 142/158/166/176/179-second markers, exact 160.999/161-second alien
  boundary, seed `20260818`, unchanged canonical plan digest, and frozen replay
  hash `09780631`.
- Seven readable rectilinear human forms at 142 seconds, the single amber
  beacon gone by 143 seconds, and no machinery revival.
- S20 must contain exactly three incomplete peripheral arcs and no full ship or
  center void. S21 and later must expose exactly three thick closed B-spline
  ribbon shells with parallel-transport frames, constrained superformula
  sections, and a genuine open center.
- The unknown ship must have no cockpit, window, panel, thruster, front, or
  other human grammar.
- The immutable ledger must stage one, few, tens, then many life lights in
  source order. High ↔ Low may alter visible density only; source ledger,
  plan, seed, story, three-shell silhouette, and center void stay unchanged.
- No post-initialize allocation, post-ready compile growth, stale chunk
  activation, pending upload, orphan lease/job, or ownership growth. The
  late-story chunk window legitimately narrows from four to three to two and
  must exactly match the desired inventory while remaining at or below four.
- The final title must be only the formal two-line Japanese title. All Hero
  geometry, material, background, fog, scene membership, and worker ownership
  must release exactly once under concurrent and repeated disposal.
- Built-in physical-material transmission remains zero; no reference image is
  shipped as a scene background and production graphics do not import the
  mutable game simulation.

## Visual review rubric

Review the WebGPU and forced-WebGL2 142-second frames anonymously, then the
158/166/176-second sequence; inspect the WebGL2 179-second title separately.
Score debris readability, rectilinear-versus-curvilinear grammar separation,
dark-space composition, nebula restraint, material/light realism, the
three-shell/open-void silhouette, alien originality, Twinkle staging, Earth
return, backend parity, fallback quality, and temporal stability independently.
A merely functional or stylized procedural scene is not sufficient for
Cinematic Procedural Realism.

## Required verdict

Return exact start/end hashes, drift status, commands, artifact statistics,
visual scores, and `ACCEPT` or `REJECT` with S0/S1/S2/S3 counts. Acceptance
applies only to R2-G5 Hero Slice C. GFX-004/GFX-005, GFX-006, Hero A/B,
reference hardware, human/rights gates, combined 180-second parity/restarts,
Main integration, Sites deployment, and contest submission remain separate.
